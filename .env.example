# Django / DRF
DJANGO_SECRET_KEY=change_me
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# DB (use SQLite for dev; can change to Postgres later)
DATABASE_URL=sqlite:///db.sqlite3

# Demo API key for simple header auth (dev only)
API_DEMO_KEY=changeme-demo-key

# Repro
RANDOM_SEED=42
